dynamics: "${include: configs/dynamics/default_dynamics.yaml, unicycle}"

horizon: 16

samplers:
  - type: "gd"
    num_steps: 100
    lr: 0.1
    bandwidth: 0.1
  - type: "multi_select_gd"
    n_base_samples: 1000
    n_children: 10
    n_child_samples: 1000
    action_std: "${dynamics.action_std}"

n_vis_samples: 1000

output_root: ???
output_name: null  # Will be generated automatically
